apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    url: ${PROMETHEUS_URL}
    isDefault: true
    editable: false
    jsonData:
      timeInterval: '10s'

  - name: Loki
    type: loki
    url: ${LOKI_URL}
    editable: false
    jsonData:
      maxLines: 1000
